{
  "name": "autoprefix-basic",
  "version": "1.0.0",
  "scripts": {
    "build":"postcss **/*.css --use autoprefixer -d build/"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.1",
    "postcss-cli": "^4.1.0"
  }
}
